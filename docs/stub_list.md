# Unresolved Stub / Mock / No-op / Placeholder List

当前无未解决项。

Gateway clean-break 删除了已下线 leave application、temporary leave approval、QC、legacy webhook/polling 和 Telegram sender 闭包。当前 Attendance Provider 的注册、个人统计、群签到/签退、群离岗/返岗、导出和班表 WebApp 路径均为生产实现；无兼容分支、no-op callback 或默认成功。
