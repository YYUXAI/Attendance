# Local unit-test example only. The shared uxassistant-test runtime does not read
# this file. Its public values come from the unified manifest and its private
# values come from root-only file bindings.

ATTENDANCE_GROUPS_JSON=[]
ATTENDANCE_GROUPS_FINGERPRINT=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
ATTENDANCE_PUBLIC_CONFIG_FINGERPRINT=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

CHECKIN_AI_ENABLED=false
CHECKIN_AI_MODE=assist
CHECKIN_AI_EXTRACT_BACKEND=ocr_only
CHECKIN_AI_CLOCK_FALLBACK_SEND_TIME=false
CHECKIN_AI_PREMIUM_ENABLED=false

GOOGLE_SHEETS_ENABLED=false
REMOTE_DIFF_GOOGLE_SHEETS_ENABLED=false
TEST_GROUP_GOOGLE_SHEETS_ENABLED=false
BBQ_GOOGLE_SHEETS_ENABLED=false

ATTENDANCE_PROVIDER_SCHEDULER_ENABLED=false
GROUP_DAILY_SUMMARY_ENABLED=false
DAILY_ATTENDANCE_REPORT_ENABLED=false
ATTENDANCE_PROVIDER_WORKER_ENABLED=false

# Do not add chat IDs, group-title lists, employee rules, credentials, database
# URLs, Spreadsheet IDs, or checkout secret paths here. Group behavior belongs
# to components.attendance.groups; employee facts belong to Attendance DB;
# private values belong to logical root-only bindings.
